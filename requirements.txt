# ============================================
# Dream Weaver 项目完整依赖清单
# ============================================
# 使用方法：
# pip install -r requirements.txt
# ============================================

# ========== Web 框架 ==========
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # 用于文件上传

# ========== 环境变量管理 ==========
python-dotenv>=1.0.0

# ========== 数据处理 ==========
pydantic>=2.0.0

# ========== 图像处理 ==========
Pillow>=10.0.0

# ========== HTTP 请求 ==========
requests>=2.31.0

# ========== Redis 缓存 ==========
redis>=5.0.0

# ========== AI 模型相关（可选，如果不需要 BLIP 可以不安装） ==========
# PyTorch（CPU 版本，如果需要 GPU 请安装对应的版本）
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# HuggingFace Transformers（用于 BLIP 模型）
transformers>=4.35.0

# 图像处理库
timm>=0.9.0

# ========== DashScope API（阿里云通义千问） ==========
dashscope>=1.14.0

# ========== 数值计算 ==========
numpy>=1.24.0

# ============================================
# 注意：
# 1. 如果不需要 BLIP 模型功能，可以不安装 torch、torchvision、transformers、timm
# 2. 如果不需要 Redis 缓存，可以不安装 redis（但需要修改代码）
# 3. 如果使用 GPU，请安装对应的 PyTorch GPU 版本
# 4. 所有依赖都已指定最低版本，建议使用最新稳定版本
# ============================================

